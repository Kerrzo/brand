---
import SectionLabel from '../ui/SectionLabel.astro';
import { withBase } from '../../lib/paths';

const services = [
  { n: '01', title: 'AI Product Advisory', copy: 'Working through where AI changes the product architecture — and where it does not.' },
  { n: '02', title: 'Prototype Sprints', copy: 'Learning by shipping a working, testable slice in weeks, not a deck about one.' },
  { n: '03', title: 'Building with AI', copy: 'Helping teams learn AI-capable ways of building through the work itself.' },
  { n: '04', title: 'Fractional CTO / Product Architecture', copy: 'Senior technical judgement when a permanent hire is not the right move yet.' },
];
---

<section class="services container">
  <div class="services__intro" data-reveal>
    <div>
      <SectionLabel>Founder Advisory</SectionLabel>
      <h2 class="services__heading">How I work with founders.</h2>
    </div>
  </div>
  <div class="services__rows" data-reveal>
    {services.map((s) => (
      <a href={withBase('/work-with-paul/')} class="services__row">
        <span class="mono-label" style="color:var(--color-label-dim)">{s.n}</span>
        <h3 class="services__row-title">{s.title}</h3>
        <p class="services__row-copy">{s.copy}</p>
      </a>
    ))}
  </div>
</section>

<style>
  .services {
    padding-top: clamp(90px, 13vh, 180px);
  }

  .services__intro {
    margin-bottom: clamp(40px, 6vh, 72px);
  }

  .services__heading {
    margin: 26px 0 0;
    font-size: clamp(36px, 5.4vw, 88px);
    line-height: .94;
    letter-spacing: -.045em;
    font-weight: 500;
    max-width: 14ch;
  }

  .services__rows {
    border-top: 1px solid var(--color-border);
  }

  .services__row {
    display: grid;
    grid-template-columns: 90px minmax(0, 1.1fr) minmax(0, 1fr);
    gap: 24px;
    padding: 30px 0;
    border-bottom: 1px solid var(--color-border);
    transition: background var(--dur-fast) ease;
  }

  .services__row:hover {
    background: rgba(14, 14, 13, .03);
    color: inherit;
  }

  .services__row-title {
    margin: 0;
    font-size: clamp(24px, 2.6vw, 40px);
    font-weight: 500;
    letter-spacing: -.03em;
    line-height: 1;
  }

  .services__row-copy {
    margin: 0;
    font-size: 15px;
    line-height: 1.6;
    color: var(--color-body);
    max-width: 44ch;
  }

  @media (max-width: 900px) {
    .services__row {
      grid-template-columns: minmax(0, 1fr);
      row-gap: 10px;
    }
  }
</style>
